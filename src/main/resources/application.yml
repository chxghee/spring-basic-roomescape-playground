server:
  port: 8080

spring:
  profiles:
    active: prod

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:database

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    ddl-auto: create

roomescape:
  auth:
    jwt:
      secret: Yn2kjibddFAWtnPJ2AFlL8WXmohJMCvigQggaEypa5E=
      expiration: 1800000
